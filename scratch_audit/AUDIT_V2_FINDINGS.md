# Card rule-audit v2 — recovered results (workflow wf_c3da82f6-451, 2026-07-01)

Audited 152 cards: 134 ok, 15 flagged as bugs. 3 audits failed (StructuredOutput cap): scholar, pavior, resource_analyzer.
Verify phase cut short by the weekly usage limit: 5 independently CONFIRMED before it stopped; the other 10 flagged cards have an audit verdict but no independent verify.

## Flagged findings (audit) + verify status

### [T1] baking_sheet — **VERIFY: CONFIRMED (high)**

**Audit:** Card text (verbatim): "Each time you take a 'Bake Bread' action, you can use this card to exchange exactly 1 grain for 2 food and 1 bonus point." Clarification: "You must bake normally to make this exchange." The reward is FLAT (−1 grain → +2 food + 1 banked point); it reads neither what the bake produced nor its chosen target. Per the official trigger-timing ruling, a bare "each time you take a 'Bake Bread' action" fires in the BEFORE window of the sub-action (before_bake_bread), and after-timing is licensed ONLY by an explicit "after" in the text or an outcome-dependent reward — neither is present. But agricola/cards/baking_sheet.py line 92 registers register("after_bake_bread", CARD_ID, _eligible, _apply). The docstring (lines 9-18, 26-31) explicitly justifies after-timing with the convenience-bias reasoning the guide names as the trap ("the after-phase is reachable only once a normal bake has been committed... auto-satisfies the 'you must bake normally' clarification"). This is exactly the T3 error: the "you must bake normally" clarification is a GATE (no bonus without a real bake), not an ordering that puts the bonus after the bake. CARD_AUTHORING_GUIDE.md §"Each time you..." (lines 205-215) names Baking Sheet BY NAME as a confirmed bug: it should be before_bake_bread. The gate is satisfied structurally on before-timing because the PendingBakeBread before-phase offers only FireTrigger + CommitBake and NO Stop (legality.py lines 1544-1560; Stop appears only in the after-phase), so a bake is still forced. Consequent T2 defect: _eligible (line 71) checks only grain >= 1 with no stranding guard — correct on before-timing it must verify a legal bake remains after the −1 grain (>= 2 grain + a baking improvement), or the now-mandatory CommitBake is stranded. Both the wrong event and the missing guard are present. Other classes checked and OK: no alternative cost (C1 n/a, no '/'), prereq _no_grain_field is a correct have-check not a spent cost (C2), flat magnitude matches text (M1), per-use CardStore bank read at scoring avoids double-count with printed 0 VP (SC1), once-per-action via triggers_resolved (S1), optional register not register_auto (O1), no animal grant (A1).

**Scenario:** Fix: change line 92 to register("before_bake_bread", CARD_ID, _eligible, _apply). Then a stranding scenario exposes the missing T2 guard: player has exactly 2 grain and a Fireplace (bakes at most... uncapped) — actually take the tighter case: player has exactly 1 grain and a baking improvement, places a worker on Grain Utilization / uses a bake host. On before_bake_bread the card is eligible (grain >= 1). Firing spends the 1 grain, leaving 0 grain. The before-phase then still forces a CommitBake (no Stop available), but with 0 grain no CommitBake(grain=n>=1) is legal — the mandatory bake is stranded / the turn dead-ends. The eligibility must instead require grain >= 2 (so >= 1 grain plus a legal baker remain for the mandatory bake). As currently shipped (after_bake_bread), the exploit is the opposite timing error: because the bonus is banked AFTER the bake with no gate on ordering, the mechanic is misfiled relative to the ruling even though the observable outcome for a lone bake happens to coincide — which is precisely the convenience-bias trap ("after just works in a test because the mandatory sub-action already ran and there is nothing left to strand").

**Verify fix_sketch:** 1) baking_sheet.py line 92: change register("after_bake_bread", CARD_ID, _eligible, _apply) → register("before_bake_bread", CARD_ID, _eligible, _apply). Update the docstring (lines 9-31) to state before-timing per the ruling, deleting the convenience-bias justification.

2) baking_sheet.py _eligible (line 67-71): add the stranding guard. Instead of `grain >= 1`, require enough grain to convert AND leave a legal bake:

    def _eligible(state, idx, triggers_resolved):
        if CARD_ID in triggers_resolved:
            return False
        p = state.players[idx]
        if p.resources.grain < 2:            # 1 to convert + >=1 for the mandatory bake
            return False
        p2 = fast_replace(p, resources=p.resources + Resources(grain=-1))
        state2 = fast_replace(state, players=tuple(
            p2 if i == idx else state.players[i] for i in range(2)))
        return _can_bake_bread(state2, state2.players[idx])

(import _can_bake_bread from agricola.legality). The state-rebuild is required because _can_bake_bread/_owns_baker derive the player index by identity against state.players, so a detached grain-reduced player would falsely fail the baker check.

3) Update tests/test_card_baking_sheet.py: rename/rework test_registered_on_after_bake_bread → before; the firing tests must fire the trigger in the before-phase (before CommitBake); add a stranding-boundary test asserting FireTrigger NOT offered at grain=1 (or grain=2 without a baker) but offered at grain≥2 with a baker, and that after firing a CommitBake remains legal.

4) Re-run C++ differential gates (pytest tests/test_cpp_*.py) if the card is ported to C++.

Sibling beer_stein.py (C61) needs the identical fix + its own stranding-guard test.
**Verify repro:** CONFIRMED — two coupled defects, both verified from code + the CARD_AUTHORING_GUIDE.md §2 ruling.

VERBATIM text: "Each time you take a 'Bake Bread' action, you can use this card to exchange exactly 1 grain for 2 food and 1 bonus point." Clarification: "You must bake normally to make this exchange." Reward is FLAT (−1 grain → +2 food + 1 banked point); it reads neither what the bake produced nor its chosen target.

RULING (CARD_AUTHORING_GUIDE.md lines 181-215): a bare "each time you take a 'Bake Bread' action" with a FLAT reward fires in the BEFORE window (before_bake_bread); after-timing is licensed ONLY by explicit "after" text or an outcome-dependent reward — neither present. The "you must bake normally" clarification is a GATE (no bonus without a real bake), not an ordering. Lines 205-215 name Baking Sheet BY NAME as shipped wrong on after_bake_bread.

DEFECT 1 (T1, wrong event): baking_sheet.py line 92 registers register("after_bake_bread", ...). Should be before_bake_bread. The docstring (lines 14-18, 26-31) justifies it with the exact convenience-bias reasoning the guide condemns ("the after-phase is reachable only once a normal bake has been committed ... auto-satisfies the clarification").

DEFECT 2 (T2, missing stranding guard): _eligible (line 71) checks only `grain >= 1`. On the correct before-timing this strands the mandatory bake. VERIFIED reproduction — I re-registered the SAME _eligible/_apply on before_bake_bread and drove the real flow:
  state: setup(seed=0), current_player=0, resources.grain=1, Fireplace (major idx 0), owns baking_sheet minor.
  actions: PlaceWorker("grain_utilization") → ChooseSubAction("bake_bread") [enters BEFORE phase; legal = [FireTrigger(baking_sheet), CommitBake(1)]] → FireTrigger(baking_sheet).
  result: grain 1→0, food +2. Then legal_actions(s) == [] — no CommitBake (needs grain≥1), and the before-phase offers NO Stop (legality.py 1544-1560; Stop only in after-phase). The turn dead-ends. This is the empty-legal-set stranding the guide's line 306-321 guard exists to prevent.

The gate IS satisfied structurally on before-timing (the before-phase forces a CommitBake — I confirmed legal_actions there = [CommitBake(1)] with no Stop and no FireTrigger under current after-registration), so DEFECT 1's fix does not need a separate "must bake normally" guard — but it DOES need DEFECT 2's stranding guard.

Verified fix: I re-registered with `grain >= 2` AND a post-spend `_can_bake_bread` recheck (building the −1-grain STATE, then _can_bake_bread(state2, state2.players[idx]) so _owns_baker resolves the index): grain=1 → not offered; grain=2 → offered, after fire grain=1 with CommitBake still legal (no strand); grain=3 → offered, no strand. Correct.

Shipped tests (tests/test_card_baking_sheet.py, 13 pass) lock in the wrong after-timing (incl. test_registered_on_after_bake_bread) and must be updated with the fix.

(Out of scope but noted: sibling beer_stein.py C61 is identical-shape and identically wrong on after_bake_bread, with a docstring that argues the anti-ruling "Using before_bake_bread would be wrong.")

### [T1] beer_stein — **VERIFY: CONFIRMED (high)**

**Audit:** Card text (verbatim): "Each time you take a 'Bake Bread' action, you can use this card once to turn 1 grain into 2 food and 1 bonus point." Clarification: "You must bake normally to make this exchange." The reward is a FLAT conversion (1 grain -> 2 food + 1 point); it does not read what the bake produced or a chosen target. Per the Trigger-Timing ruling, a bare "each time you take a 'Bake Bread' action" fires in the BEFORE window -> should be `before_bake_bread`. beer_stein.py line 79 instead registers `register("after_bake_bread", CARD_ID, _eligible, _apply)`. The docstring (lines 16-21, "Using before_bake_bread would be wrong...") uses the "must bake normally" clarification to justify after-timing — this is exactly the T3 error: the clarification is a GATE (no bonus without a real bake), not an ordering that licenses after_. This is not ambiguous: CARD_AUTHORING_GUIDE.md lines 205-215 name Beer Stein AND Baking Sheet explicitly — "Both cards shipped on after_bake_bread on exactly the wrong reading of this clarification; that is the mistake this paragraph exists to stop." Baking Sheet is a CONFIRMED T1 bug in the prompt, and beer_stein.py is byte-for-byte the same shape (docstring line 8: "IDENTICAL mechanics to Baking Sheet"). Established correct pattern: potter_ceramics.py and hand_truck.py (also "each time you take a Bake Bread action" cards) both register on `before_bake_bread`. The gate is satisfied structurally by before-timing: the PendingBakeBread before-phase enumerator (legality.py lines 1544-1560) offers only FireTriggers + CommitBake with NO Stop, so a bake is still forced. Secondary defect (T2, manifests once the event is fixed to before): `_eligible` (line 58) only checks `grain >= 1`, but a before-trigger that spends 1 grain must leave a legal bake remaining afterward (>=2 grain AND a baker), per guide lines 212-213 — otherwise the mandatory CommitBake is stranded when the player has exactly 1 grain. Fix: change line 79 to before_bake_bread and tighten _eligible to require grain >= 2 and _owns_baker(state, p).

**Scenario:** Player owns Beer Stein and a Fireplace, has exactly 2 grain, and takes Grain Utilization -> bake_bread. With correct before-timing, firing Beer Stein converts 1 grain to 2 food + 1 point BEFORE the mandatory bake, leaving 1 grain that must then be baked. The current after_bake_bread implementation gets the resource/point totals right in that specific test only because the bake already ran, but it MODELS THE WRONG GAME EVENT: (a) it lets the player bake ALL their grain first and still exchange (they must have kept grain back for the exchange, i.e. cannot bake grain #2), inflating output; and (b) on any delegating host, after-timing places the exchange after the action-space window has closed, contrary to the enforce-first rule that before_action_space/before_<sub> triggers must fire before the base sub-action. Concretely with grain=2: correct engine forces a choice — bake 1 + exchange 1 (net: 1 loaf's worth baked, +2 food, +1pt) — whereas after-timing lets the player bake 2 grain into 2 loaves AND still not exchange, or (in the test's own path) bake 1 then exchange the other, whichever maximizes; the before-gate constrains this, the after-gate does not.

**Verify fix_sketch:** Two changes in agricola/cards/beer_stein.py, mirroring the sibling cards potter_ceramics.py / hand_truck.py:
1) Line 79: register("after_bake_bread", ...) → register("before_bake_bread", CARD_ID, _eligible, _apply). This fires the flat exchange BEFORE the (still-mandatory, no-Stop) base bake; the "must bake normally" gate is satisfied structurally because the before-phase enumerator forces a CommitBake.
2) Line 58 _eligible: change `state.players[idx].resources.grain >= 1` to require a legal bake to survive spending 1 grain — `grain >= 2 and _owns_baker(state, p)` (import _owns_baker from agricola.legality as hand_truck/potter do), so the exchange never strands the mandatory bake (finite-cap baker + exactly 1 grain case).
Update the docstring (lines 8-33) to state before-timing + the stranding guard, and rewrite tests/test_card_beer_stein.py: assert registration on before_bake_bread; drive the before-phase (fire, then mandatory CommitBake); add the strand-guard boundary tests (grain=1-with-Clay-Oven not offered; grain>=2 offered). No cost/scoring change (1 clay; +1 banked point via CardStore + register_scoring stay correct).
**Verify repro:** Confirmed real bug (T1 wrong-event + T2 stranding-guard defect), verified from code + rules.

VERBATIM: "Each time you take a 'Bake Bread' action, you can use this card once to turn 1 grain into 2 food and 1 bonus point." Clarification: "You must bake normally to make this exchange." The reward is a FLAT conversion (reads nothing the bake produced) → must fire before, per CARD_AUTHORING_GUIDE §2 ("Flat reward → before"; the guide names Beer Stein/Baking Sheet explicitly at lines 205-226 as the exact mistake it exists to stop). Sibling "each time you take a Bake Bread action" cards potter_ceramics.py:90 and hand_truck.py:92 both correctly use before_bake_bread; beer_stein.py:79 uses after_bake_bread.

REPRO (T1, ran end-to-end, PYTHONPATH=. python):
  s = setup(seed=0); with_current_player(s,0); with_resources(s,0,grain=2); with_majors(owner_by_idx={0:0}) [Fireplace]; own beer_stein.
  step PlaceWorker("grain_utilization"); step ChooseSubAction("bake_bread").
  OBSERVED before-phase options = [CommitBake(1), CommitBake(2)] — FireTrigger NOT offered pre-bake.
  step CommitBake(grain=2) → grain=0, food=4, after-phase=[Stop] only (exchange gone).
This lets the player bake ALL grain into 2 loaves and forgo the exchange — modeling the exchange as an optional add-on AFTER the whole bake, not as part of it. Correct before-timing forces the single line: FireTrigger(beer_stein) first (-1 grain, +2 food, +1pt) THEN mandatory CommitBake(grain=1) (before-phase has no Stop — legality.py:1544-1560 appends Stop only in the after-phase).

REPRO (T2 stranding, once event is fixed to before): Beer Stein + ONLY a Clay Oven (cap 1, no uncapped Fireplace/Hearth) + grain=1. Current _eligible (beer_stein.py:58) checks only grain>=1, so it fires → grain 1→0. Before-phase then computes max_grain=min(0,1)=0 → the `for n in range(1,1)` loop emits ZERO CommitBake actions, and the before-phase never appends Stop → stranded frame with NO legal action (the exact stranding the guide's lines 212-213 warn about).

Also: tests/test_card_beer_stein.py locks in the bug (test_registered_on_after_bake_bread + after-phase driver) and must be rewritten with the fix.

### [T1] bucksaw — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Verbatim text: "Each time you renovate, you can also pay 1 wood to get 1 bonus point and 1 grain." This is a bare "each time you renovate" with a FLAT reward (pay 1 wood -> get 1 bonus point + 1 grain); it reads NOTHING about what the renovate produced (no target material, no room count, no outcome dependence). Per the official Trigger-Timing ruling (CARD_AUTHORING_GUIDE.md lines 181-203: a bare "each time you [do X]" fires in the BEFORE window of X for sub-actions incl. renovate, unless text says "after"; "Flat reward -> before; outcome-dependent reward -> after"), the correct event is before_renovate. The implementation registers on after_renovate (bucksaw.py line 62), and the docstring (lines 23-25) justifies it explicitly by "matches the renovate-hook convention (Mining Hammer / Roughcaster / Millwright all hook the renovate after-phase)" — this is exactly the after_ CONVENIENCE BIAS the guide names as the single most common timing bug. The comparison cards are legitimately after because they are outcome-dependent (Roughcaster detects a clay->stone renovate, roughcaster.py line 41; skillful_renovator reads people placed) or grant a separate primitive (Mining Hammer's free stable) — none is a flat reward like bucksaw. The before_renovate event is real and wired: legality.py line 1719 ("before-phase offers before_renovate triggers + the renovate commit(s)") and cards/__init__.py lines 417-419 reference before_renovate firing directly. So bucksaw should register on before_renovate. NOTE (secondary, T2): the fix also needs a stranding guard — current _eligible (bucksaw.py line 41) only checks wood >= 1, but on before_renovate the 1-wood charge is spent before the mandatory renovate whose own cost includes wood, so eligibility must verify a legal renovate remains after the -1 wood (else the forced renovate is stranded).

**Scenario:** A player owns Bucksaw and takes a House Redevelopment / renovate action. Correct rules (before-timing): the "pay 1 wood -> 1 point + 1 grain" offer is presented as part of the renovate action, before the renovate resolves, and the 1 wood it costs competes with the wood the renovate itself needs. As implemented (after_renovate), the offer only appears after the renovate has already been fully paid and applied, so the player never faces the shared-wood tension the card creates and the trigger cannot interact with the renovate's own payment. Concrete divergence: a player with just enough wood to renovate but not enough to also feed Bucksaw is, under the correct before-timing, forced to choose (and the stranding guard must forbid taking the bonus if it would leave the mandatory renovate unpayable); under the current after-timing that interaction is silently absent, changing which states can fire the bonus and the payment ordering — a rule-observable difference in exactly the borderline-wood cases the card is about.

### [T1] corn_schnapps_distillery — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Verbatim text (revised_minor_improvements.json, Corbarius C64): "Once per round, you can pay 1 grain to place 1 food on each of the next 4 round spaces. At the start of these rounds, you get the food." No clarifications. The card is a once-per-round *anytime-during-the-round* action. In Agricola, "once per round" (with NO "at the start of each round" qualifier) means usable at any point during the round, contrasted with cards that explicitly restrict to round start. The same catalog proves the distinction: Plow Driver A90 and Groom B89 both say "at the start of each round" (start-of-round-restricted); Clay Carrier D122 says "At any time, but only once per round"; Tea House / Sour Dough / Mummy's Boy tie their "once per round" to person placement DURING the turn. Corn Schnapps says only "Once per round" — anytime.

The implementation (corn_schnapps_distillery.py lines 77-78) registers the effect ONLY as a `start_of_round` trigger + `register_start_of_round_hook(CARD_ID)`, with no during-turn availability (confirmed: the sole registrations are the start_of_round trigger and hook; nothing else references the card). Its FireTrigger is surfaced only at the `PendingPreparation` host, which `engine._fire_preparation_hook` (engine.py:1146-1157) pushes and resolves at round start BEFORE any worker placement. So the effect is confined to the start-of-round window. The docstring itself models it "like Plow Driver / Groom" — but those cards carry the explicit "at the start of each round" restriction that this card's text lacks; the Plow Driver pattern was copied without that textual distinction.

Note the schedule math IS correct: at fire time `_complete_preparation` has already set round_number to the entered round R (step 3, engine.py:1051) before firing the hook (step 5), and _apply uses range(R+1, R+5) = R+1..R+4, matching sibling Category-8 cards (wall_builder/pond_hut use range(R+1, R+1+N)). The once-per-round `used_this_round` latch (line 51,62) and its clear in _complete_preparation are also correct. All other classes check out: cost 1 Wood/2 Clay has no "/" alternative (C1 ok); grain is a spent Resources cost debited directly (C2 ok); 1 food x4 / 1 grain / 1 vps magnitudes correct (M1 ok); declinable "you can" trigger with Proceed decline (O1 ok); used_this_round scope correct (S1 ok); no mandatory sub-action to strand grain (T2 n/a); no immediate animals / family growth / double-count scoring / harvest conversion / counting (A1/G1/SC1/H1/D1 n/a). The single divergence is the availability window.

**Scenario:** A player owns Corn Schnapps Distillery and enters round R with 0 grain. Under the correct "once per round" (anytime) rule, they place a worker on the permanent "grain_seeds" space during round R, gaining 1 grain, then activate Corn Schnapps later that same round to pay 1 grain and place 1 food on rounds R+1..R+4. The implementation never offers this: at the start-of-round `PendingPreparation` host, `_eligible` returns False because grain==0, and once that host is Proceeded away (before any worker placement) there is no further point in round R where the FireTrigger can be surfaced. The player permanently loses a legal, strategically-relevant line (acquire grain mid-round, then convert it into 4 future food) despite satisfying the card's actual once-per-round condition.

### [T1] dutch_windmill — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Card text (verbatim): "Each time you take a 'Bake Bread' action in a round immediately following a harvest, you get 3 additional food." This is a FLAT reward — a fixed +3 food that reads nothing about the bake's outcome, chosen target, or produced food. In agricola/cards/dutch_windmill.py, _eligible (line 28-29) gates only on `state.round_number in _POST_HARVEST_ROUNDS`, and _apply (line 32-36) simply adds `Resources(food=3)`; neither inspects any post-bake state. Per the governing ruling (CARD_AUTHORING_GUIDE.md, "Each time you take/use/do X" fires in the BEFORE window of X unless the text literally says "after"/"immediately after"), a flat "each time you take a Bake Bread action" reward must fire on `before_bake_bread`. The word "after" does not appear in the text — "immediately following a harvest" modifies the ROUND, not the reward's timing. But line 40 registers on `after_bake_bread`: `register_auto("after_bake_bread", CARD_ID, _eligible, _apply)`. This is precisely the confirmed beer_stein / baking_sheet bug class (both flat "each time you take a Bake Bread action" effects wrongly placed on after_bake_bread; should be before_bake_bread) — the same `after_` convenience bias. There is NO stranding justification (T2) to excuse the after placement, since +3 food consumes no resource the mandatory bake needs (unlike beer_stein's grain conversion) — a before placement strands nothing, so the reason beer_stein's docstring gave for after does not even apply here. Correct fix: change line 40 to register_auto("before_bake_bread", ...). Engine confirms the divergence is real: before_bake_bread autos fire when PendingBakeBread is pushed (engine.py _fire_subaction_before_auto, right after ChooseSubAction("bake_bread"), before the player sees CommitBake); after_bake_bread autos fire only at _enter_after_phase after CommitBake (resolution.py:1120) — so under the current code the +3 income arrives at the rules-incorrect moment. Other classes checked and clear: cost "2 Wood, 2 Stone" has no "/"|"or" (C1 ok); no supply prereq (C2 ok); round set {5,8,10,12,14} is correctly derived ({4,7,9,11,13,14}+1 capped at 14; round 14 correctly included as it follows round 13's harvest) (D1/H1 ok); flat +3 not per-unit/banded (M1 ok); automatic is correct for a no-downside pure-goods grant (O1 ok); once-per-bake-action scope correct (S1 ok); no animal/family/replacement/scoring issues (A1/G1/R1/SC1 ok).

**Scenario:** Player owns Dutch Windmill and, in round 5 (immediately following the round-4 harvest), takes a Bake Bread action via Grain Utilization, baking 1 grain. Correct rules timing: the moment the player takes the Bake Bread action (before window), they get +3 food, so the +3 is in supply while the bake sub-action is being resolved and available to interact with any other before_bake_bread effect and with the bake decision itself. Current code: the +3 food is withheld until _enter_after_phase, i.e. only after CommitBake completes — the income arrives one window too late. The divergence is observable whenever another before_bake_bread effect or the bake-amount decision co-occurs (e.g. a player also owning a before_bake_bread grain-supplier like Hand Truck): with the correct before placement the two effects share the same pre-commit window, whereas the current after placement sequences Dutch Windmill's income strictly after the committed bake, producing a rules-incorrect ordering. General case: any bake in rounds {5,8,10,12,14} gets its +3 at the wrong phase relative to the "each time you take the action" ruling.

### [T1] loppers — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** VERBATIM text: "Each time you build 1 or more fences, you can also use this card to exchange 1 wood and 1 fence in your supply for 2 food and 1 bonus point." This is a FLAT exchange (1 wood + 1 fence -> 2 food + 1 point) — it reads NOTHING about what the build produced (how many fences, which pastures), only the "1+ fence built" gate. Per the Trigger-Timing ruling (CARD_AUTHORING_GUIDE.md lines 183-203), a bare "each time you build X" flat reward fires in the BEFORE window; `after_` is legitimate ONLY when the effect must read what X produced or its target (Shepherd's Crook / trimmer / asparagus_gift all correctly diff a before/after snapshot because they ARE outcome-dependent — count/threshold/subdivision-exclusion). loppers.py line 78 registers on `after_build_fences`.

The docstring (loppers.py lines 8-13) justifies after-timing with exactly the forbidden reasoning: "the build_fences host flips to its after-phase (reached via Proceed, which itself requires pastures_built >= 1 — so 'you build 1 or more fences' is satisfied by construction, no extra fence-count guard needed)." The guide names this the `after_` convenience-bias trap (lines 200-203: "Do not use 'I put it after so the state was settled' as a reason... after_ 'just works' in a test because the mandatory sub-action already ran and there is nothing left to strand"). This is structurally identical to the CONFIRMED beer_stein/baking_sheet bugs — flat exchanges wrongly on `after_bake_bread`, should be `before_bake_bread` with a stranding guard. The gate "1 or more fences" is a GATE not a license for after-timing (guide lines 205-215, T3), and it is satisfied structurally in the before-window anyway: the fencing delegating host forces ChooseSubAction("build_fences") with no Stop (legality.py 1792,1828-1830), and PendingBuildFences' before-phase offers no Proceed/Stop until pastures_built>=1 (legality.py 2102-2103), so you can never leave with 0 fences — the same structural forcing Beer Stein relies on.

The correct event is `before_build_fences`, WITH a stranding guard (guide lines 306-321): loppers spends 1 wood AND 1 fence-from-supply, both of which the mandatory fence build needs. Eligibility (loppers.py lines 48-54) currently checks only `wood>=1 and fences_in_supply>=1` — it does NOT verify a legal fence commit remains after the -1 wood / -1 fence. The contention is real: `_enumerate_pending_build_fences` gates buildable pastures on `fences_left = buildable_fences(p)` (legality.py 2068) which = `p.fences_in_supply + free_fence_pool_remaining(p)` (helpers.py 34), and on wood — so spending the loppers wood+fence before the build reduces what can be enclosed. After-timing hides this shared-resource interaction entirely, and additionally makes the exchange unavailable to a player who spent their full supply on the build (see scenario).

**Scenario:** Player owns Loppers (+2 occupations), reaches Fencing with wood=4, fences_in_supply=4, and a farmyard whose only maximal enclosure needs exactly 4 new edges (4 wood, 4 fences). AS IMPLEMENTED (after_build_fences): they build the 4-edge pasture (wood->0, fences_in_supply->0); the after-phase loppers `_eligible` then sees wood<1 and fences_in_supply<1 and cannot fire — the exchange is silently forfeited, and it never competed with the build. CORRECT (before_build_fences): loppers must be offered before the build; firing it debits 1 wood + 1 fence-from-supply, leaving 3 wood / 3 fences so the 4-edge pasture is no longer buildable — forcing the real tradeoff the "exchange 1 fence in your supply" wording intends (the fence piece is consumed and thus unavailable to build with). The two timings yield different legal action sets and different reachable farm states: after-timing wrongly lets a player both build to the limit AND (when they have slack) take a cost-free exchange that never contends with the enclosure, whereas the rules make the wood and fence piece a shared resource with the build.

### [T1] mining_hammer — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Card text (verbatim): "When you play this card, you immediately get 1 food. Each time you renovate, you can also build a stable without paying wood." The renovate grant is registered on the AFTER phase: mining_hammer.py line 53 `register("after_renovate", CARD_ID, _eligible, _apply)`. Per the Trigger-Timing ruling (CARD_AUTHORING_GUIDE.md lines 181-203): a bare "each time you [do X]" — including for the renovate sub-action — fires in the BEFORE phase; after-timing is legitimate ONLY when the effect must read what X produced or X's chosen target. The granted effect here is a free stable build, whose eligibility (`_can_build_stable`, legality.py lines 336-349) depends solely on an empty farmyard cell + stable supply + payment — NONE of which a renovate changes (renovate mutates only `house_material` and resources, `_execute_renovate` resolution.py lines 1347-1349). So the grant is a FLAT, outcome-INDEPENDENT primitive grant and must fire on `before_renovate`, not `after_renovate`. This is the same `after_` convenience-bias class confirmed on beer_stein/baking_sheet. Contrast Roughcaster (roughcaster.py, "renovate your house from clay to stone" — genuinely outcome-dependent, correctly on after_renovate). The module docstring's stated justification — "Mirrors Threshing Board / Assistant Tiller (after-trigger that grants a primitive)" (mining_hammer.py lines 13-14) — is factually wrong: threshing_board.py line 43 and assistant_tiller.py line 40 are both `before_action_space` triggers, as are every comparable bare-"each time you use/do" grant (cottager.py line 79, oven_firing_boy.py line 41). The renovate before-phase already fires before_renovate triggers (`_enumerate_pending_renovate`, legality.py lines 1721-1730), so before_renovate is a supported event and the fix is machinery-aligned. No stranding guard is needed after the move because the free stable consumes no resource the mandatory renovate needs. All other bug classes checked and clean: cost "1 Wood" has no alternative (C1 ok); grant is optional via `register` not `register_auto` (O1 ok); once-per-renovate scope via `triggers_resolved` (S1 ok); +1 food on play and single free stable magnitudes match text (M1 ok); stable placed through the accommodation-aware PendingBuildStables primitive, no immediate animal grant (A1 ok).

**Scenario:** Correctness divergence per the ruling (observable in interaction / ordering, and a correctness error regardless): a player renovates while owning both Mining Hammer and another before_renovate trigger. With Mining Hammer wrongly on after_renovate, the two grants resolve in the wrong window relative to the base renovate — the base renovate (a mandatory before-phase commit) closes the before-window and forces the commit, then Mining Hammer's stable is offered only afterward, whereas the rules put the "each time you renovate" grant in the before-window (before the renovate's own effect resolves). Concretely: the enforce-first mechanic for before_renovate triggers (a grant the player wants must fire BEFORE using the renovate) is bypassed for Mining Hammer, so any card that should interact with Mining Hammer's stable in the before-window (or condition on state prior to the renovate commit) sees the wrong ordering. Even in isolation the phase is a fixed correctness decision by the text+ruling, not a convenience choice ("when the observable outcome would coincide either way, still classify by the ruling" — CARD_AUTHORING_GUIDE.md lines 190-191).

### [T1] rocky_terrain — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Card text (verbatim): "Each time you plow a field (tile or card), you can also buy 1 stone for 1 food." This is a bare "each time you [do X]" trigger whose reward is FLAT — buy 1 stone for 1 food is a fixed goods exchange that does NOT read what the plow produced or its chosen cell. Per the official Trigger-Timing ruling in CARD_AUTHORING_GUIDE.md lines 195-203 ("Flat reward -> before; outcome-dependent reward -> after"; the `after_` convenience bias "is the single most common timing bug"), a flat "each time you [do X]" must fire in the BEFORE window: `before_plow`. The implementation registers on `after_plow` instead (rocky_terrain.py line 81: `register("after_plow", CARD_ID, _eligible, _apply)`), and the docstring's own justification (lines 12-14: "the plow is the TRIGGER and the +1-stone purchase is the REWARD, so the event is `after_plow` (NOT `before_action_space`)") is exactly the convenience-bias reasoning the guide names as a bug. This is directly analogous to the CONFIRMED beer_stein / baking_sheet bugs (flat "each time you take a Bake Bread action" conversions wrongly placed on after_bake_bread; corrected to before_bake_bread). The legitimate after_plow users differ: barrow_pusher.py uses after_plow because it must detect a field TILE WAS CREATED (outcome-dependent, mandatory automatic) — rocky_terrain's flat purchase has no such dependency. No stranding concern justifies after either: the plow sub-action consumes no food, so a before_plow version needs no stranding guard (T2 inapplicable). All other bug classes checked OK: cost is a single "1 Food" (no "/" alternative), purchase is correctly liquidation-aware, +1 stone once per plowed field is the right magnitude and scope (triggers_resolved on per-field PendingPlow), optionality correct (mandatory=False, Stop declines).

**Scenario:** Timing divergence on a delegating/interleaved plow host. Under the ruling, the +1-stone-for-1-food buy must be offered in the plow's BEFORE window (before the field cell is placed). With the current after_plow registration, the buy is offered only AFTER the plow resolves. On a delegating host or across a multi-field plow (Cultivation / Swing/Turnwrest/Wheel Plow "plow up to N fields"), the ordering of the buy relative to the plow and any other before-window triggers is load-bearing: after-timing can enable subsidizing/interaction patterns that before-timing (enforce-first: taking the mandatory sub-action closes the before-window) blocks — the same class of divergence that made the Moldboard Plow and Writing Desk timing fixes necessary. Concretely, moving the food-spend + stone-gain from after-the-plow to before-the-plow changes the state the plow (and any co-fired before-plow triggers) sees, so the observable game state differs whenever another before-window effect on the same host reads or competes for the spent food / gained stone.

### [T1] roughcaster — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Card text (A110): "Each time you build at least 1 clay room OR renovate your house FROM CLAY TO STONE, you also get 3 food." The renovate clause is correctly on `after_renovate` (outcome-dependent — the guide names Roughcaster as the legitimate after-timing case, CARD_AUTHORING_GUIDE.md lines 195-198), but its eligibility predicate reads an INSUFFICIENT outcome signal. roughcaster.py:40-42 `_eligible_renovate` returns `state.players[idx].house_material == HouseMaterial.STONE`. The comment claims "STONE ⟺ a clay->stone renovate" — true ONLY in the base game where CLAY→STONE is the sole path to stone (legality.py:670-671). But Conservator (occupation A87, implemented, `players 1+`) registers a renovate-target extension making WOOD→STONE a legal renovation directly (conservator.py:25-31, legality.py:663-679). Both are occupations, so one player can own both. On a Conservator WOOD→STONE renovate, `_execute_renovate` sets house_material=STONE (resolution.py:1347-1348), and at the after_renovate phase `_eligible_renovate` sees STONE and returns True → +3 food granted. But a wood→stone renovate is NOT "from clay to stone", so Roughcaster must NOT fire. PendingRenovate does not persist the pre-renovate material (pending.py:256-276; to_material rides only on CommitRenovate.payment), so the after-phase predicate cannot distinguish the source material as written. The predicate should gate on the FROM→TO transition (clay→stone), not merely the resulting STONE material. All other classes checked OK: +3 food is flat/once-per-session (M1 ok, `_apply` grants Resources(food=3) once), auto-effect is correct for a pure-goods gain (O1 ok), after-timing avoids stranding (T2 n/a), no cost string / harvest / animal / count issues.

**Scenario:** Player owns both Roughcaster (A110) and Conservator (A87) occupations, with a WOOD house and enough stone+reed. They place a worker on House Redevelopment, ChooseSubAction("renovate"), and CommitRenovate(to_material=STONE) — legal because Conservator adds WOOD→STONE as a target (legality.py:675-678). _execute_renovate sets house_material=STONE and pivots PendingRenovate to its after-phase. `_eligible_renovate(state, idx)` returns STONE==STONE → True, so `_apply` grants +3 food. Per the card text ("renovate your house from clay to stone"), a wood→stone renovation is not a clay→stone renovation, so Roughcaster should give NOTHING here. The player illegitimately gains 3 food.

### [T2] drill_harrow — **VERIFY: CONFIRMED (high)**

**Audit:** Card text (verbatim): "Each time before you take an unconditional 'Sow' action, you can pay 3 food to plow 1 field." Timing (T1) is correctly modeled: register("before_sow", ...) at drill_harrow.py:68 matches the literal "before", and it is optional (register/FireTrigger) matching "you can". Cost/magnitude/scope all check out (cost 1 wood = Resources(wood=1); _FOOD_COST=3; plow 1 field; once-per-sow via triggers_resolved).

The defect is T2 STRANDING. The before_sow trigger fires from INSIDE the PendingSow frame's before-phase (legality.py:1505, _enumerate_pending_sow). At that point the player is already committed to the sow: the before-phase offers only FireTrigger + CommitSow(grain,veg) — no Stop/Proceed exit (Stop appears only in the after-phase, line 1501-1503). CommitSow requires grain+veg>=1 (line 1490/1515-1518). drill_harrow's eligibility guard (drill_harrow.py:48-53) is `_can_plow(p) and _liquidatable_to(state, idx, p, Resources(food=3))`. It gates on the plow target and on the 3 food being RAISABLE by liquidation, but `_liquidatable_to`/food_payment_frontier freely convert GRAIN and VEG to food (legality.py:462-467, and the frontier at 2543-2556 spends grain/veg as fuel) with NO reservation of the >=1 seed the mandatory sow needs. So when the 3 food must be raised by liquidating the very grain/veg being sown, firing the trigger strands the sow with an empty legal set on a non-empty stack — a dead state.

This is exactly the danger beer_stein.py:20-21 documents ("...could deplete grain the bake itself needs"); beer_stein sidesteps it by using after_bake_bread, but drill_harrow's text mandates before_sow, so the correct fix is a stranding guard (ensure grain+veg>=1 survives paying 3 food, i.e. reserve one seed unit / require an empty field remains plus a seed the liquidation won't touch) — analogous to _can_plow_twice guarding cooperative_plower/moldboard_plow. The guard is missing.

**Scenario:** Player owns drill_harrow, is at PendingSow before-phase (sow already chosen from Grain Utilization/Cultivation — fully reachable in normal play), holds exactly 3 grain, 0 veg, 0 food, no animals, one empty field + a plowable cell. Reproduced deterministically against the engine (seed 1, prefab): _eligible returns True (_can_plow True; _liquidatable_to True since 3 grain -> 3 food covers owe=3). Fire drill_harrow -> pushes PendingFoodPayment(food_needed=3). The only frontier option is CommitFoodPayment(grain=3) (must burn all 3 grain to raise 3 food). After it: grain=0, veg=0, food=0; PendingPlow resolves (CommitPlow then Stop), leaving PendingSow before-phase with grain+veg=0. legal_actions(PendingSow) returns [] — empty legal set with a non-empty stack = engine dead-end. Control (do not fire): CommitSow(grain=1,veg=0) is available and the sow proceeds normally, confirming the strand is drill_harrow-specific.

**Verify fix_sketch:** Add the missing seed-preservation stranding guard so a fired trigger can never empty grain+veg below the >=1 the mandatory CommitSow needs (analogous to _can_plow_twice + must_preserve_base for granted plows, and to reserved_animals in _liquidatable_to).

Eligibility (drill_harrow.py:48-53): require the 3 food be liquidatable while leaving one seed unit (grain OR veg) intact —
  _can_plow(p) and (
     _liquidatable_to(state, idx, p, Resources(food=_FOOD_COST, grain=1))
     or _liquidatable_to(state, idx, p, Resources(food=_FOOD_COST, veg=1)))
(_liquidatable_to already reserves the non-food component: cost.grain=1 forces grain>=1 on hand at line 453 AND excludes it from the conversion pool at line 460.)

Execution (_apply, drill_harrow.py:56-64): when short on food, push PendingFoodPayment with the matching reservation so the frontier cannot burn the reserved seed — reserved=Cost(resources=Resources(grain=1)) (or veg=1, mirroring whichever branch the gate satisfied), instead of reserved=Cost(). Keep the direct-pay path (food>=3) unchanged.

This is a gate<->frontier-consistent pair (the same governing invariant _liquidatable_to's docstring calls out): the guard marks the card playable only when the food-payment frontier — run over goods minus the reserved seed — is still non-empty, so no gate/frontier mismatch and no strand. Verified the guard rejects the 3g/0v case while keeping 3g+1v (keep veg) and 4g/0v (burn 3, keep 1) playable.
**Verify repro:** Deterministically reproduced against the live engine (repro at /private/tmp/claude-501/-Users-hunterwieman-Desktop-Agricola-AgricolaBot/c580c2bd-2e29-4c2e-9325-7a26aeaeab44/scratchpad/repro_drill_harrow.py, reusing tests/test_cards_pay_food_plow.py helpers).

State: P0 owns drill_harrow; reached PendingSow before-phase via normal play (PlaceWorker("grain_utilization") -> ChooseSubAction("sow")); inventory EXACTLY grain=3, veg=0, food=0, no animals; one empty FIELD to sow + a plowable EMPTY cell. (grain_for_sow=1, extra_grain=2 in the harness.)

Trace (actual engine output):
- drill_harrow._eligible -> True (_can_plow True; _liquidatable_to(food=3) True: 3 grain @1:1 -> 3 food).
- PendingSow before-phase legal_actions = [FireTrigger(drill_harrow), CommitSow(grain=1,veg=0)]  (no Stop/Proceed).
- step FireTrigger(drill_harrow) -> pushes PendingFoodPayment(food_needed=3, reserved=Cost()); legal = [CommitFoodPayment(grain=3)] (only frontier point burns all 3 grain).
- step CommitFoodPayment(grain=3) -> grain=0, food=0; pushes PendingPlow.
- step CommitPlow(...) then step Stop() pops the granted plow -> back at PendingSow before-phase with grain+veg=0.
- legal_actions(state) == []  with a NON-EMPTY pending stack (top=PendingSow) => fatal dead-end. No engine recovery: _legal_actions_uncached (legality.py:2770) returns the top frame's enumeration, here []; any agent/MCTS loop halts.

CONTROL (do not fire): CommitSow(grain=1,veg=0) is available and the sow proceeds normally — confirming the strand is caused solely by firing drill_harrow. drill_harrow-specific: sibling pay-food-plow cards hook before_action_space/placement hosts that all expose Proceed()/decline, so they never strand; drill_harrow is the only one on before_sow (a mandatory CommitSow with no decline path).

### [T2] teachers_desk — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** test

**Scenario:** test

### [C1] chophouse — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** C1 ALTERNATIVE COST bug — identical to the confirmed club_house case (same Bubulcus expansion, same misencoding). Chophouse's printed cost is "2 Wood/2 Clay" (revised_minor_improvements.json line 1997 / card_text output). In this data set the `/` is unambiguously an ALTERNATIVE (OR) separator, while `,` is the AND (combined) separator — proven by sibling costs: "Return Fireplace/Cooking Hearth" (return one OR the other), "1 Wood,4 Clay/2 Stone" and "2 Wood/2 Clay,1 Reed" (comma combines, slash alternates), "2 Wood/2 Clay/2 Stone" (three alternatives). So "2 Wood/2 Clay" means pay 2 Wood OR 2 Clay, never both. The card's own text also uses `/` this way ("Grain/Vegetable Seed", "3/2 round spaces"). The implementation (agricola/cards/chophouse.py line 53) encodes it as `register_minor(CARD_ID, cost=Cost(resources=Resources(wood=2, clay=2)), vps=1)`, i.e. wood=2 AND clay=2. The play path (resolution.py:558-559 `p.resources - pay`) and the cost enumeration (legality.py:1318 `base = spec.cost.resources`) treat this as a single combined base with no alternative branch — Cost has no `alternatives` field and register_minor has no alternatives param, so the engine cannot express OR. The player is charged 4 building resources (2 wood + 2 clay) instead of the correct 2 (either 2 wood or 2 clay). All other classes checked and OK: T1 timing correct (before_action_space for the flat "each time you use" scheduling reward); T2 no stranding (trigger only schedules future food via schedule_resources, consumes nothing the seed sub-action needs); M1 magnitude correct (3 rounds grain / 2 rounds veg, 1 food each = "next 3/2 round spaces"); S1 scope correct (per-use before_action_space); SC1 scoring correct (1 vps, no double-count).

**Scenario:** A player with 2 wood and 0 clay (or 0 wood and 2 clay) should be able to build Chophouse by paying their 2 wood (or 2 clay). Under the current encoding, playable_minors → can_pay runs the combined cost Resources(wood=2, clay=2): the player lacks the other resource, so can_pay returns False and Chophouse is illegal for them, even though the rules permit paying the single alternative they hold. Conversely, a player with 2 wood and 2 clay who plays Chophouse has BOTH subtracted (resources - Resources(wood=2, clay=2) at resolution.py:559), leaving them 4 building resources poorer instead of 2 — overpaying by a full alternative every time.

### [O1] mini_pasture — **VERIFY: CONFIRMED (med)**

**Audit:** Mini Pasture (B002) is a TRAVELING / passing-left minor improvement, but it is registered as a KEPT card. The verbatim card data in agricola/cards/data/revised_minor_improvements.json (the Mini Pasture entry, deck B #2) carries `"passing_left": "X"`. That "X" is the engine's marker for a passing minor: every one of the 8 currently-implemented passing minors — market_stall, wood_pile, food_basket, game_trade, clay_embankment, plant_fertilizer, shifting_cultivation, young_animal_market — has `passing_left="X"` in that same JSON field AND is registered with `passing_left=True` in its card module. Mini Pasture has the identical `"X"` marker, so it is a traveling card by the same rule; in real Agricola, after you play a traveling minor and resolve its immediate effect you pass it to the opponent, never keep it in your tableau. The MinorSpec docstring (agricola/cards/specs.py:134-135) states this exactly: "passing_left — a traveling minor: executed then passed to the opponent, NEVER kept in the tableau." But the registration in agricola/cards/mini_pasture.py:60-61 is `register_minor(CARD_ID, cost=Cost(resources=Resources(food=2)), prereq=_can_fence_new_1x1, on_play=_on_play)` with NO `passing_left=True`, so it defaults to False (specs.py:161). Runtime confirms: MINORS['mini_pasture'].passing_left == False (vs market_stall == True). Consequently in _execute_play_minor (agricola/resolution.py:562-570), the `if not spec.passing_left:` branch runs and the card is added to the owner's `minor_improvements` tableau and NOT circulated to the opponent's hand — the wrong lifecycle for a traveling card. (All other bug classes checked and clean: cost "2 Food" has no alternative and is encoded correctly; the free_fence_budget=4 correctly covers a 1x1's <=4 edges while pieces still draw from supply; the exact_size=1/forbid_subdivision/max_pastures=1 geometry and adjacency handling match the text; the mandatory grant matches the owner ruling; no counting/animal/scoring/magnitude issue; the effect is an on_play immediate effect, not a before/after action-space trigger, so T1/T2/T3 timing windows don't apply.)

**Scenario:** Player 0 plays Mini Pasture from hand for 2 food and fences a free 1x1 pasture. Correct Agricola behavior: after the fence is placed, the Mini Pasture card is passed to Player 1 (it enters Player 1's hand and Player 0 no longer holds it). Actual behavior: because passing_left defaults to False, _execute_play_minor keeps 'mini_pasture' in Player 0's minor_improvements tableau and never adds it to Player 1's hand_minors — so the card is stuck with the original owner instead of traveling, denying the opponent the chance to play it later and mis-tracking who has the card.

**Verify fix_sketch:** Add `passing_left=True` to the register_minor call in agricola/cards/mini_pasture.py:60-61, mirroring market_stall.py:29-33:

  register_minor(CARD_ID,
                 cost=Cost(resources=Resources(food=2)),
                 prereq=_can_fence_new_1x1,
                 passing_left=True,
                 on_play=_on_play)

This routes the play through the existing `if spec.passing_left:` branch in _execute_play_minor (resolution.py:565-570): the card is removed from the owner's hand, NOT added to the owner's minor_improvements tableau, and added to the opponent's hand_minors — the correct traveling lifecycle. No new machinery needed; the primitive-composing on_play (push PendingBuildFences with the FenceRestrictions 1x1 grant) still runs identically (spec.on_play at resolution.py:581 fires whether kept or passed). The prereq _can_fence_new_1x1 correctly re-gates playability each time the card circulates back (traveling cards "stay in circulation indefinitely"). Update the module docstring (which currently describes it as a kept card) and add a lifecycle assertion to tests/test_cards_mini_pasture.py (card leaves owner's hand/tableau, enters opponent's hand_minors) so the traveling behavior is locked in.
**Verify repro:** Confirmed real bug (O1: wrong card lifecycle — traveling minor kept instead of passed).

Rules ground truth: RULES.md §"Passing (traveling) minors" (lines 588-595) states traveling minors are "the lowest-numbered minor improvements, numbers 001-009, in each deck" — played, effect resolved, then "pass it to the player on your left, who takes it into their hand, rather than keeping it in your tableau." Mini Pasture is deck B #2 (B002), squarely in that range, and its JSON (agricola/cards/data/revised_minor_improvements.json) carries passing_left="X". I proved "X" is a RELIABLE marker: it is present on EXACTLY the 45 cards numbered 001-009 in decks A-E and absent everywhere else (X-set == 001-009-set, empty symmetric difference). So per the rules Mini Pasture must be passed to the opponent, not kept.

Code defect: agricola/cards/mini_pasture.py:60-61 calls register_minor(...) with NO passing_left kwarg, so it defaults to False (specs.py:145,161). Runtime: MINORS['mini_pasture'].passing_left == False (vs market_stall/shifting_cultivation == True). In _execute_play_minor (resolution.py:562-570) the `if not spec.passing_left:` branch (562-563) then adds 'mini_pasture' to the owner's minor_improvements tableau, and the `if spec.passing_left:` pass-to-opponent branch (565-570) is skipped.

Concrete reproduction (ran it): setup(seed=0); give P0 food=2 and hand_minors={'mini_pasture'}, clear P1's hand; push PendingPlayMinor(player_idx=0); then _execute_play_minor(state, 0, CommitPlayMinor(card_id='mini_pasture', payment=Resources(food=2))). Result: P0.minor_improvements == {'mini_pasture'}, P0.hand_minors == {}, P1.hand_minors == {}. Expected (traveling): P0.minor_improvements == {}, P1.hand_minors == {'mini_pasture'}. So the card is stuck with the owner and never travels to the opponent.

NOT a design decision. CARD_BATCH_TRIAGE.md:657 claims "X" is unreliable because it "appears on kept cards (Mini Pasture, Sleeping Corner, Field Fences)" — but that evidence is false: Sleeping Corner is A#26 (passing_left=None) and Field Fences is C#16 (passing_left=None); neither carries "X". Mini Pasture (B002) is the only cited example and it IS a genuine 001-009 traveling card. The same doc self-corrects at line 664 (Upscale Lifestyle B001: "passing_left=True ... the card travels to the opponent"). The tests (tests/test_cards_mini_pasture.py) only cover the fencing grant, never the keep/pass lifecycle, so nothing locks in the wrong behavior.

Severity med: only reachable in Cards (beta) mode (Family unaffected, C++ gates untouched); it mis-tracks ownership and denies the opponent a card that should re-enter circulation, but it is a card-mode-only rules deviation, not an engine/crash issue.

### [S1] tasting — **VERIFY: missing (weekly limit) — needs manual confirmation**

**Audit:** Card text (verbatim, card_text.py): "Each time you use a \"Lessons\" action space, before paying the occupation cost, you can exchange 1 grain for 4 food." The bonus is scoped SPECIFICALLY to a Lessons action space.

Implementation (tasting.py:63) registers on the GENERIC event: `register("before_play_occupation", CARD_ID, _eligible, _apply)`, and `_eligible` (tasting.py:42-44) checks ONLY `CARD_ID not in triggers_resolved and state.players[idx].resources.grain >= 1` — it never checks WHICH route pushed the PendingPlayOccupation frame. So it fires on EVERY occupation play, not just Lessons.

The `before_play_occupation` event is emitted by every `PendingPlayOccupation` frame's before-phase (trigger_event() in legality.py:2264-2273; `_eligible_fire_triggers` legality.py:2276-2295; enumerator `_enumerate_pending_play_occupation` legality.py:2395). Multiple IMPLEMENTED, REGISTERED cards push that frame via NON-Lessons routes, each with a distinguishing `initiated_by_id`:
 - Lessons (resolution.py:488): `initiated_by_id="space:lessons"` — the ONLY route the card text covers.
 - Private Teacher (private_teacher.py:76): pushes PendingPlayOccupation via the "Grain Seeds" action space — text: "Each time you use the \"Grain Seeds\" action space ... you can also play an occupation".
 - Forestry Studies (forestry_studies.py:85): plays an occupation via the "Forest" accumulation space.
 - Scholar (scholar.py:72): plays an occupation at the start of a round (no action space at all).

Result: a Tasting owner who plays an occupation via Private Teacher / Forestry Studies / Scholar is wrongly offered the 1-grain→4-food exchange, contradicting the card's explicit "a \"Lessons\" action space" scope.

This is NOT the Paper Maker pattern (which tasting.py's docstring copied). Paper Maker's verbatim text is "Immediately before playing EACH occupation after this one..." — genuinely route-agnostic, so firing on all `before_play_occupation` events is correct for Paper Maker. Tasting's text is narrower and was over-generalized. The frame already carries the field needed to gate this (`PendingPlayOccupation.initiated_by_id`, pending.py:678); a correct fix restricts `_eligible` to `state.pending_stack[-1].initiated_by_id == "space:lessons"`.

Other classes checked and OK: T1 timing (before-window: correct, text says "before paying the occupation cost"); T2 stranding (firing ADDS food net +4, can only make the play more affordable — never strands the mandatory play, and commit re-gates on _payable); C1/C2 cost (2 Wood, no "/"/"or", encoded Resources(wood=2) — correct); M1 magnitude (1 grain→4 food exact); O1 optionality ("you can" → declinable register trigger, decline path tested); D1/H1/A1/G1/R1/SC1 not applicable.

**Scenario:** Player owns Tasting and Private Teacher (registered occ). Opponent has a worker on Lessons; player uses the "Grain Seeds" action space and fires Private Teacher to play an occupation via its 1-food route (PendingPlayOccupation with initiated_by_id="card:private_teacher"). That frame's before-phase fires `before_play_occupation`; Tasting's `_eligible` returns True (grain>=1, not yet resolved), so `FireTrigger(card_id="tasting")` is offered and the player exchanges 1 grain for 4 food — even though no Lessons action space was used, violating the card's "Each time you use a \"Lessons\" action space" scope. Same over-fire occurs when playing an occupation via Forestry Studies (Forest space) or Scholar (start-of-round).

### [SC1] luxurious_hostel — **VERIFY: CONFIRMED (med)**

**Audit:** Card text (verbatim, revised_minor_improvements.json #34): "During scoring, if you then have more stone rooms than people, you get 4 bonus points. You can only use one card to get bonus points for your stone house." The trailing sentence is a cross-card MUTUAL-EXCLUSION rule: among cards granting a "bonus points for your stone house" bonus, a player may use only ONE. luxurious_hostel.py registers an independent scoring term (register_scoring(CARD_ID, _score), line 61) and scoring.py sums every owned scoring term unconditionally: card_points = sum(fn(state, player_idx) for card_id, fn in SCORING_TERMS if _owns(ps, card_id)) (scoring.py ~238-240). The card's docstring (lines 18-22) claims "No sibling such card is implemented today, so this clause is inert and a standalone scoring term is faithful." That premise is FALSE: half_timbered_house.py IS implemented and carries the IDENTICAL clause verbatim ("During scoring, you get 1 bonus point for each stone room you have. You can only use one card to get bonus points for your stone house." — revised C30) and is a registered minor with its own scoring term (confirmed: both luxurious_hostel and half_timbered_house are in MINORS and SCORING_TERMS). So a player owning BOTH scores 4 + (1 per stone room), when the rules permit only the larger of the two. The mutual-exclusion group is not enforced anywhere; the two card docstrings each (wrongly) assert the other is not implemented. All other bug classes check out: cost "1 Wood,2 Clay" -> Cost(wood=1, clay=2) is correct (comma = both, no '/'); the comparison is correctly STRICT (_stone_rooms > people_total); "people" = people_total (home+placed) is the correct reading and newborns are cleared by scoring time; stone-only room count mirrors scoring.py exactly; flat 4 (not per-unit/banded); vps=0 with the conditional 4 in the scoring term (no self double-count); ownership-scoped via _owns. It is a pure end-game scoring term, so timing/stranding/harvest/animal/optionality classes are N/A.

**Scenario:** A player plays BOTH Luxurious Hostel and Half-Timbered House (both are minors in the D/C decks; nothing prevents co-ownership). At end game they live in a stone house with, say, 5 stone rooms and 3 people. Engine scores: Luxurious Hostel = 4 (5 rooms > 3 people) PLUS Half-Timbered House = 5 (1 per stone room) = +9. Correct per rules: the player may use only ONE card for the stone-house bonus, so they take max(4, 5) = +5, not +9. The engine over-scores by 4 points (the smaller of the two stone-house bonuses is illegally also counted). The mismatch flips whenever a player owns both stone-house-bonus cards.

**Verify fix_sketch:** Introduce a mutual-exclusion group into the scoring machinery. Add an optional group key to register_scoring, e.g. register_scoring(card_id, fn, group="stone_house_bonus"), and give both cards that group. In scoring.py's card_points computation, replace the flat unconditional sum with: sum grouped terms by taking only the MAXIMUM over each group's owned+applicable terms (max(fn(state,idx) for owned members of the group)), and continue summing ungrouped terms normally. So for the stone-house group a co-owner gets max(luxurious_hostel._score, half_timbered_house._score) instead of the sum. Also fix both docstrings (they each wrongly claim the other card is unimplemented) to state the group is now enforced. Add a regression test: a stone house with N rooms > people owning BOTH cards scores +max(4, N), not +(4+N).
**Verify repro:** Verified real bug (SC1: cross-card mutual-exclusion not enforced). Both stone-house-bonus cards carry the identical verbatim clause "You can only use one card to get bonus points for your stone house" — Half-Timbered House (C30) and Luxurious Hostel (D34) — and BOTH are implemented and registered (confirmed: both in MINORS and in SCORING_TERMS). scoring.py (~238-241) sums every owned scoring term unconditionally with no group/exclusive concept, so a player owning both illegally banks the sum instead of the max.

Concrete repro (run against the engine, factories from tests/):
  import agricola.cards; from agricola.scoring import score; from agricola.setup import setup
  from agricola.constants import CellType, HouseMaterial; from agricola.state import Cell
  from tests.factories import with_grid, with_house, with_minors, with_people
  s = setup(seed=7)
  # P0: STONE house, 5 ROOM cells, 3 people
  ov = {(1,0):Cell(),(2,0):Cell()}
  for rc in [(1,0),(2,0),(0,0),(1,1),(2,1)]: ov[rc]=Cell(cell_type=CellType.ROOM)
  s = with_house(with_grid(s,0,ov),0,HouseMaterial.STONE); s = with_people(s,0,total=3)
Results (delta over owning neither, baseline total 2):
  own luxurious_hostel only  -> +4  (5 stone rooms > 3 people, strict)
  own half_timbered only     -> +5  (1 per stone room x5)
  own BOTH (engine)          -> +9   <-- WRONG
  rules-correct (max 4,5)    -> +5
Engine over-scores by +4 whenever a player owns both cards and both bonuses are positive. Neither test file (test_card_luxurious_hostel.py / test_card_half_timbered_house.py) covers co-ownership; both docstrings falsely assert the sibling is not implemented (mutually contradictory). All other bug classes N/A/correct: cost Cost(wood=1,clay=2) correct; comparison correctly STRICT; people=people_total correct; stone-only room count mirrors scoring.py; flat 4; vps=0; ownership-scoped via _owns.
