Read the following files before doing anything else:

1. `CLAUDE.md` — project status, per-file descriptions, key design principles
2. `agricola/state.py` — ground truth for all field names and dataclass shapes
3. `RULES.md` — complete rules reference for the 2-player Family game

Do not implement anything yet. After reading, confirm you understand the current state of the codebase and flag any questions.

During the session, after implementing any code changes, ask the user if they should be documented in SESSION_HISTORY.md and/or any other .md documents. Repeat this once after each new significant code change (anything more than a small tweak), even if the user has ignored these questions previously.